{
  "name": "lefinor-capital-website",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio web de Lefinor Capital Group. Generador estático propio (Node.js) que combina datos JSON con plantillas HTML. Sin frameworks ni CMS; una única dependencia de build (qrcode-generator) para los códigos QR de las tarjetas digitales.",
  "scripts": {
    "build": "node generator/build.js",
    "watch": "node generator/watch.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "qrcode-generator": "^2.0.4"
  }
}
