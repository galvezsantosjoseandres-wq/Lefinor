{
  "name": "lefinor-capital-website",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio web de Lefinor Capital Group. Generador estático propio (Node.js) que combina datos JSON con plantillas HTML. Sin frameworks ni CMS; dependencias de build: qrcode-generator (códigos QR de las tarjetas digitales) y tailwindcss (CSS compilado en build time).",
  "scripts": {
    "build": "node generator/build.js",
    "watch": "node generator/watch.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "qrcode-generator": "^2.0.4",
    "tailwindcss": "^3.4.19",
    "vcards-js": "^2.11.1"
  }
}
